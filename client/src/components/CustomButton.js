import makeStyles from '@material-ui/core/styles/makeStyles';
import CustomButton from './CustomButton.styles';

const CustomButtonStyles = makeStyles(CustomButton);

export { CustomButton, CustomButtonStyles };

export { default } from './CustomButton.styles';